"""Bundled, read-only StormPad resources.

Files here ship inside ``StormPad.app`` and are copied into a user's workspace
through the normal storage layer. Nothing in this package is ever written to,
and the app never depends on it after a workspace has been initialized.
"""
