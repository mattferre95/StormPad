"""Note data structures.

Pure, AppKit-free domain model. Implemented in Phase 2.
"""

from __future__ import annotations

# TODO(phase-2): Note dataclass — id, path, title, body, transcript blocks,
# category, created/updated timestamps — plus a clean, serializable shape that
# WisperFlow can later reuse (see PRD §13).
